"""Cooper's own LLM Tools API and its tools.

These supplement HA's built-in Assist API. The Assist API grounds the model in the
home (exposed entities/areas/floors and the intent tools to control them); Cooper's
API fills the gaps Assist does not cover: recalling history, looking at cameras,
durable preference memory, authoring automations/scripts, and proactive watches.
"""
